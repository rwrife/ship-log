"""ship-log: a git-native captain's log for the multi-agent era.

An append-only, plain-text ledger that lives inside your repo (``.shiplog/``)
where every decision, attempt, and dead-end gets a one-line "why" — so the next
agent (or you, tomorrow) can skip the graveyard of already-tried ideas.
"""

__version__ = "0.1.0"

__all__ = ["__version__"]
